{
  "first": 36,
  "last": 40,
  "discourse": "Alright, so everything I've said so far about choosing model complexity has come with a caveat: *if* we have a solid theory to guide us. But let's be honest with ourselves — very often we don't. So here's the practical question: suppose we don't have a solid theory. What then?\n\nThe answer is on this slide: use cross-validation results to guide the choice of model complexity. And which cross-validation technique you use depends on how much data you have. If the dataset is small, use leave-one-out cross validation. If the dataset is large enough, use k-fold cross validation or repeated-random-sampling validation. Let's take these one at a time.\n\nFirst, leave-one-out cross validation. Let D be the original data set. We start with `testResults` as an empty list, and then, for i in range of the length of D — so, once for every single example in D — we do the following: we take a copy of D and pop out the i'th element; whatever remains is our training set. We build a model on that training set, then we test that model on the one example we held out, D of i, and append the result to testResults. So every example in the dataset takes its turn being the test set, all by itself. When the loop is done, we average the test results, and that average is our estimate of model performance.\n\nNotice what this costs you: you build one model per data point. A thousand examples, a thousand models. That's exactly why this is the technique for *small* datasets — it would be prohibitively expensive on large ones.\n\nWhich brings us to k-fold, which is very similar, and applies when we have a large amount of data. Instead of holding out one example at a time, we partition D into k equal-size sets. The model is trained on k minus 1 of those sets, and tested on the remaining set. So now we only build k models instead of one per example — much more tractable when the data is plentiful.\n\nThe third option is repeated random sampling. Again, let D be the original data set. Now n is the number of random samples — and usually n is between 20% and 50% of the data — and k is the number of trials. We initialize testResults to the empty list, and for i in range k — so, for each of k trials — we randomly select n elements to serve as the test set, and keep the rest for training. We build the model on the training data, test it on the test set, append the result, and after all k trials, we average the test results. Notice that here the split is re-drawn randomly on every trial, so unlike k-fold we're not carefully partitioning the data — we're just sampling over and over.\n\nGood. Now let's make all of this concrete with an example: temperature by year. The task is to model how the mean daily high temperature in the U.S. varied from 1961 through 2015. Here's the plan. First, get the means for each year and plot them. Then — instead of eyeballing the plot and guessing at the right complexity — we randomly divide the data in half, n times. For each dimensionality to be tried — each candidate complexity of our model — we train on one half of the data, test on the other half, and record the r-squared on the test data. Finally, we report the mean r-squared for each dimensionality, and that's what guides our choice. You should recognize this as exactly the repeated-random-sampling idea, with the split set at fifty-fifty.\n\nOf course, before we can do any of that, we have to get the data into a usable form. And here it is — I call this \"a boring class,\" because you've seen this pattern a dozen times by now. We define class tempDatum. The initializer takes a string s, splits it on commas, and extracts two fields: self.high is the float of info at index 1 — the daily high temperature — and self.year is the int of the first four characters of info at index 2 — the year the reading was taken. Then, because we're well-behaved object-oriented programmers, we don't reach into the instance variables directly; we provide accessor methods. getHigh returns self.high, and getYear returns self.year. That's it. Nothing clever going on — just a clean little container, so the rest of our code can simply ask each datum for its year and its high temperature, and we can get on with the interesting part: computing the yearly means and running the validation experiment.",
  "wikipedia_data": "## Cross-validation (statistics)\nCross-validation ,    sometimes called rotation estimation    or out-of-sample testing , is any of various similar model validation techniques for assessing how the results of a statistical analysis will generalize to an independent data set. Cross-validation includes resampling and sample splitting methods that use different portions of the data to test and train a model on different iterations. It is often used in settings where the goal is prediction, and one wants to estimate how accurately a predictive model will perform in practice. It can also be used to assess the quality of a fitted model and the stability of its parameters. In a prediction problem, a model is usually given a dataset of known data on which training is run ( training dataset ), and a dataset of unknown data (or first seen data) against which the model is tested (called the validation dataset or testing set ).   The goal of cross-validation is to test the model's ability to predict new data that was not used in estimating it, in order to flag problems like overfitting or selection bias  and to give an insight on how the model will generalize to an independent dataset (i.e., an unknown dataset, for instance from a real problem). One round of cross-validation involves partitioning a sample of data into complementary subsets, performing the analysis on one subset (called the training set ), and validating the analysis on the other subset (called the validation set or testing set ). To reduce variability , in most methods multiple rounds of cross-validation are performed using different partitions, and the validation results are combined (e.g. averaged) over the rounds to give an estimate of the model's predictive performance. In summary, cross-validation combines (averages) measures of fitness in prediction to derive a more accurate estimate of model prediction performance.  Assume a model with one or more unknown parameters , and a data set to which the model can be fit (the training data set). The fitting process optimizes the model parameters to make the model fit the training data as well as possible. If an independent sample of validation data is taken from the same population as the training data, it will generally turn out that the model does not fit the validation data as well as it fits the training data. The size of this difference is likely to be large especially when the size of the training data set is small, or when the number of parameters in the model is large. Cross-validation is a way to estimate the size of this effect. [ citation needed ] In linear regression, there exist real response values y 1 , … , y n {\\textstyle y_{1},\\ldots ,y_{n}} , and n p -dimensional vector covariates x 1 , ..., x n . The components of the vector x i are denoted x i 1 , ..., x ip . If least squares is used to fit a function in the form of a hyperplane ŷ = a + β T x to the data ( x i , y i ) 1 ≤ i ≤ n , then the fit can be assessed using the mean squared error (MSE). The MSE for given estimated parameter values a and β on the training set ( x i , y i ) 1 ≤ i ≤ n is defined as: If the model is correctly specified, it can be shown under mild assumptions that the expected value of the MSE for the training set is ( n − p − 1)/( n + p + 1) < 1 times the expected value of the MSE for the validation set (the expected value is taken over the distribution of training sets). Thus, a fitted model and computed MSE on the training set will result in an optimistically biased assessment of how well the model will fit an independent data set. This biased estimate is called the in-sample estimate of the fit, whereas the cross-validation estimate is an out-of-sample estimate. [ citation needed ] Since in linear regression it is possible to directly compute the factor ( n − p − 1)/( n + p + 1) by which the training MSE underestimates the validation MSE under the assumption that the model specification is valid, cross-validation can be used for checking whether the model has been overfitted , in which case the MSE in the validation set will substantially exceed its anticipated value. (Cross-validation in the context of linear regression is also useful in that it can be used to select an optimally regularized cost function .) In most other regression procedures (e.g. logistic regression ), there is no simple formula to compute the expected out-of-sample fit. Cross-validation is, thus, a generally applicable way to predict the performance of a model on unavailable data using numerical computation in place of theoretical analysis. Two types of cross-validation can be distinguished: exhaustive and non-exhaustive cross-validation. Exhaustive cross-validation methods are cross-validation methods which learn and test on all possible ways to divide the original sample into a training and a validation set. Leave- p -out cross-validation ( LpO CV ) involves using p observations as the validation set and the remaining observations as the training set. This is repeated on all ways to cut the original sample on a validation set of p observations and a training set.  LpO cross-validation require training and validating the model C p n {\\displaystyle C_{p}^{n}} times, where n is the number of observations in the original sample, and where C p n {\\displaystyle C_{p}^{n}} is the binomial coefficient . For p > 1 and for even moderately large n , LpO CV can become computationally infeasible. For example, with n = 100 and p = 30, C 30 100 ≈ 3 × 10 25 . {\\displaystyle C_{30}^{100}\\approx 3\\times 10^{25}.} A variant of LpO cross-validation with p=2 known as leave-pair-out cross-validation has been recommended as a nearly unbiased method for estimating the area under ROC curve of binary classifiers.  Leave- one -out cross-validation ( LOOCV ) is a particular case of leave- p -out cross-validation with p = 1. The process looks similar to jackknife ; however, with cross-validation one computes a statistic on the left-out sample(s), while with jackknifing one comput",
  "figures": [
    {
      "url": "https://upload.wikimedia.org/wikipedia/commons/thumb/6/61/Confusion_matrix.png/500px-Confusion_matrix.png?utm_source=en.wikipedia.org&utm_campaign=parser&utm_content=thumbnail",
      "caption": "Comparing the cross-validation accuracy and percent of false negative (overestimation) of five classification models. Size of bubbles represent the standard deviation of cross-validation accuracy (USD).",
      "article": "Cross-validation (statistics)",
      "article_url": "https://en.wikipedia.org/wiki/Cross-validation_%28statistics%29"
    },
    {
      "url": "https://upload.wikimedia.org/wikipedia/commons/thumb/b/b5/K-fold_cross_validation_EN.svg/500px-K-fold_cross_validation_EN.svg.png?utm_source=en.wikipedia.org&utm_campaign=parser&utm_content=thumbnail",
      "caption": "Diagram of k-fold cross-validation",
      "article": "Cross-validation (statistics)",
      "article_url": "https://en.wikipedia.org/wiki/Cross-validation_%28statistics%29"
    },
    {
      "url": "https://upload.wikimedia.org/wikipedia/commons/thumb/c/c7/LOOCV.gif/960px-LOOCV.gif?utm_source=en.wikipedia.org&utm_campaign=parser&utm_content=thumbnail",
      "caption": "Illustration of leave-one-out cross-validation (LOOCV) when n = 8 observations. A total of 8 models will be trained and tested.",
      "article": "Cross-validation (statistics)",
      "article_url": "https://en.wikipedia.org/wiki/Cross-validation_%28statistics%29"
    },
    {
      "url": "https://upload.wikimedia.org/wikipedia/commons/thumb/4/4b/KfoldCV.gif/960px-KfoldCV.gif?utm_source=en.wikipedia.org&utm_campaign=parser&utm_content=thumbnail",
      "caption": "Illustration of k-fold cross-validation when n = 12 observations and k = 3. After data is shuffled, a total of 3 models will be trained and tested.",
      "article": "Cross-validation (statistics)",
      "article_url": "https://en.wikipedia.org/wiki/Cross-validation_%28statistics%29"
    }
  ]
}