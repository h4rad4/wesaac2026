# Secrets
.env

# External inputs, except the one course the paper's experiment runs on, which ships with
# the repo so a reader can reproduce without hunting the slides down.
data/*
!data/README.md
!data/Introduction to Computational Thinking and Data Science/

# Run outputs. Regenerated by process_deck; never redistributed.
results/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
.ipynb_checkpoints/
